services:
  test_job_69_77:
    container_name: test_job_69_77
    image: test_job_img
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /root/projects/test:/app/output

networks:
  default:
    external: true
    name: reverse-proxy


